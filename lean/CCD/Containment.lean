import Mathlib
import CCD.Degradation

/-!
# Graphical criterion for containment

Formalizes the containment results:
* **Prop. "Graphical criterion for containment"** — if
  `(P ∖ P̃_{𝓜_u}) ∩ de_{𝒢_u}(Y) = ∅` then the intervention `u` contains the attack.
* **Prop. "Complexity of checking containment"** — the criterion is checkable in
  `O(|V| + |U| + |𝓔|)` time.

TODO: theorem statements and proofs to be added once the proof details are provided.
-/

namespace CCD

-- TODO: `theorem containment_of_disjoint ...` and the complexity bound.

end CCD

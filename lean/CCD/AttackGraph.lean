import Mathlib

/-!
# Attack graph

Formalizes the attack graph `Γ = ⟨P, E, 𝒱⟩` (Sec. "Attacker Model"): privileges `P`
(OR-nodes), vulnerability exploits `E` (AND-nodes), and the bipartite edge set `𝒱`
encoding exploit pre- and post-conditions, together with the assumption that the
attacker's privileges accumulate monotonically.

TODO: definitions and lemmas to be added once the proof details are provided.
-/

namespace CCD

-- TODO: `structure AttackGraph` and monotone privilege accumulation.

end CCD
